#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
#include <string>
#include <stdexcept>

using namespace std;
bool quadFormula(double varA , double varB, double varC, double *quadResult)
{
  double quadra = sqrt( (varB* varB) - (4*varA*varC) );

  if (!varA)
  {
      cout << "UNAVAILABLE"\n;
      return false;

  }












}




int main()
{
    double varA;
    double varB;
    double varC;
    double [2]
    cout << "The quadratic equation is ax^2 + bx + c = 0" << endl;
    cout << "Enter the variables for a , b , and finally c followed by with 'Enter' after all the values have been given." << endl;

    cin >> varA >> varB >> varC;


























}
